
import 'package:flutter/material.dart';

class RegisterDesktopLayout extends StatelessWidget {
  const RegisterDesktopLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Center(child: Text("The desktop layout has not been implemented yet.",style: TextStyle(color: Colors.white,fontSize: 30),)),
      ],
    );
  }
}
